export default function EvaluationDocs() {
 return <><h1>Find, try and review an agent</h1>
 <h2>1. Find a match</h2><p>Open /workbench and describe your task in English using 15–500 characters. Include your required capabilities and constraints. DeepSeek extracts search concepts, retrieves up to 48 relevant published descriptions and ranks up to four matching agents. Cards show one short reason, an exact description quote and up to two unknowns. There is no generated essay. You can also browse the catalog and filters below the search.</p>
 <p>Selection uses self-declared metadata, not proven task performance. The server rejects unknown IDs, duplicate selections and quotes missing from the source. The model is instructed to exclude weak alternatives; semantic relevance is not guaranteed by the quote check. Empty results mean no supported recommendation was returned.</p>
 <h2>Live Graph evidence before ranking</h2><p>Sepolia is the default search network. Arc Testnet uses a separate Graph index with the same evidence checks. Before ranking, the server reads fresh registry data from The Graph and compares ownership and metadata URI with the exact published snapshot block. Changed ownership, a changed or empty URI, missing historical evidence or a missing indexed record removes the candidate before DeepSeek receives it. The index must be within 15 minutes of the current time. An unavailable, stale or erroneous index pauses recommendations on the affected network instead of falling back to old descriptions.</p><p>DeepSeek receives the remaining candidates with indexed feedback counts, distinct feedback wallets, URI updates and transfers. It uses these facts as context for task suitability and uncertainty, and must cite one to three supplied registry signals for each Sepolia or Arc match. The server rejects unsupported signal IDs. Counts include withdrawn feedback and do not prove independent customers or completed jobs. The contents at an unchanged HTTP URI can still change; Try this agent performs the separate document check. Ethereum and Arbitrum Sepolia matches are clearly marked as snapshot-only; the trial check still reads current Arbitrum ownership and metadata directly from chain 421614. Arc covers the 249 explicitly listed catalog IDs, refreshed at block 61,552,064 on September 11, 2026; it is not a full registry index. Each network has its own evidence status and indexed block.</p><h2>2. Choose Try this agent</h2><p>The app reads current ownership and metadata URI at a common block, compares available metadata hashes and probes up to three declared public HTTPS URLs. A response shows reachability at that time, not successful task execution. Missing legacy hashes stay unknown.</p>
 <p>The trial guide shows eligible observed links. An A2A card or JSON document is labeled as a service document, not a chat interface; an external client may be needed. Authentication and payment requirements are shown. If no usable link was confirmed, choose another agent or inspect the provider instructions in its record. Expired or held evidence disables these links until rechecked.</p>
 <h2>3. Prepare and run a small trial</h2><p>Add a short public or synthetic sample. The app combines it with your original task in a copyable trial template. This template is assembled locally, not generated by another AI request. The sample stays in page memory and is lost on reload. Copying does not submit a task: you choose whether to paste it into the provider’s interface. Confirm costs and permissions before sending it.</p>
 <p>Run the trial with the provider. Verify the requested output, calculations or sources and whether your limits were respected. NOMEN does not execute A2A/MCP calls, send private data to the agent, sign transactions or authorize payments.</p>
 <h2>4. Record and recheck</h2><p>Save the evidence report to My shortlist. To record a successful trial review, provide a result note of at least 20 characters and complete every review checkbox. Evidence must be fresh, have a usable provider link and have no failed or changed checks. A manual review may be recorded while task fit is still unknown; it remains your report, not a NOMEN certification.</p>
 <p>Reports expire after 15 minutes. Ownership, metadata or service changes invalidate previous reviews. Rechecking is manual; nothing monitors agents when the page is closed. Up to 20 entries and eight previous observations per entry are stored in this browser. Exported JSON is unsigned and is not a transferable attestation.</p>
 <h2>Live decision versus user review</h2><p>Evaluation decisions are shortlist_for_trial, needs_review and hold. Full live checklist evidence can support a read-only shortlist; a changed owner or document pauses it. Custom task performance remains unknown until a human tests it. Legacy checklist matching uses declared keywords; the separate discovery stage uses DeepSeek. automaticExecutionAllowed is always false, including after a manual review.</p>
 <h2>Provider configuration and errors</h2><p>The default is the direct DeepSeek API with model deepseek-flash and a server-only DEEPSEEK_API_KEY. NOMEN_DISCOVERY_MODEL can override it; supported alternative provider IDs route through Vercel AI Gateway. Each AI call uses bounded structured output. An invalid structured response gets one retry within the shared 45-second deadline. Provider failures return an error, not invented matches.</p>
 <p>Requests have size limits, a maximum of three concurrent searches and best-effort per-instance throttling of six searches per minute per IP. These are not a durable global spending cap. A 429 means throttling or a provider limit; a 503 means matching could not finish. Wait and retry, or use the catalog.</p>
 <h2>Privacy and scope</h2><p>Your search text, candidate descriptions and public registry facts are sent to DeepSeek. Keep secrets and confidential documents out of the search. The app does not intentionally log prompts; provider retention policies apply. Trial sample editing and clipboard copying are local until you independently share the content. Saved notes and exports may contain what you enter.</p>
 <p>Live URL checks use public IPv4 HTTPS on port 443, bounded responses and no followed redirects. A failed fetch can reflect infrastructure limitations; it is not evidence of fraud. ENS naming, optional x402 settlement and live The Graph indexing are separate from the trial workflow.</p>
 <h2>API integration</h2><pre><code>{`POST /api/discover
{"request":"Find an agent for weather forecasts and air quality","chain":"sepolia"}

POST /api/evaluate
{"chain":"ethereum","agentId":22817,"job":"custom","request":"Find an agent for weather forecasts and air quality"}`}</code></pre><p>Use the exact task and chain-qualified agent ID from the discovery result. Do not reuse a review for another task. See the API reference for response fields and limits.</p></>;
}
