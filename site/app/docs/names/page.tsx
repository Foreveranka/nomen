export default function Names() {
 return <><h1>Names on ENSv2</h1><p>The replacement registrar is deployed and verified on Sepolia. Claim a name under nomen-demo.eth using a passing agent you own. The demo name reader.nomen-demo.eth belongs to NOMEN-owned test agent #10226. These are Sepolia test names, not mainnet ENS names.</p><h2>Claim</h2><p>The current ERC-8004 owner supplies a label and Merkle proof. The registrar checks ownership, current eligibility, suspension and existing registration. Labels contain 3–32 lowercase ASCII letters, digits or hyphens, with no leading or trailing hyphen. The name receives no owner transfer or resolver-edit roles.</p><h2>Current endorsement</h2><p>isNamed returns true only when the current registration generation is unexpired, ownership still matches, the agent is not suspended and its checked root matches the current published root. Publishing a root invalidates old endorsements until revalidated. This is not automatic monitoring of metadata.</p><h2>Resolver records</h2><p>NomenResolver restricts writes to the registrar, isolates records by registration token and returns empty text when endorsement expires or is withdrawn. A failed record write rolls back a claim. The resolver exposes the text and ERC-165 interfaces; universal compatibility with every ENS client has not been established.</p><h2>Revocation and renewal</h2><p>An administrator can revoke with a public reason. Revocation suspends the agent, so the same proof cannot immediately reclaim a name. Reinstatement requires an explicit administrator action with a current proof. Renewal also requires a current proof and matching ownership. The administrator remains a trust assumption.</p><pre><code>{`(bool live, string memory label, uint64 expiry) = registrar.isNamed(agentId);
require(live, "no current NOMEN endorsement");`}</code></pre></>;
}
